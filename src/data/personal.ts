import type { Reference, Track } from '../types/content';
// Estas secciones se muestran al agregar contenido real autorizado por el propietario.
export const music: Track[] = [];
export const references: Reference[] = [];
